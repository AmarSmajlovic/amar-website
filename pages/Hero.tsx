/* eslint-disable react/jsx-no-undef */
/* eslint-disable react/no-unescaped-entities */
import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";
import styled from "styled-components";
import { HeroImageAmar } from "../assets";
import { FlexContainer, Text, Container, Button } from "../styles";
import { SocialIcons, TypeWriter } from "../components";

const ImageComponent = styled(Image)`
  filter: drop-shadow(5px 10px 5px rgb(0 0 0 / 0.4));
`;

const Hero: NextPage = () => {
  return (
    <Container>
      <Head>
        <title>Amar's Website</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <FlexContainer flexDirection="column" gap="10px">
        <ImageComponent
          priority
          quality={100}
          src={HeroImageAmar}
          alt="amar-hero-image"
        />
        <Text as="i" bolder>
          SOFTWARE DEVELOPER
        </Text>
        <TypeWriter />
        <SocialIcons />
        <Button bolder size="lg" variant="primary">
          GET IN TOUCH
        </Button>
      </FlexContainer>
    </Container>
  );
};

export default Hero;
